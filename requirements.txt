# School Daylight dependencies
# Pinned to exact versions so pip install today = same result in six months

pandas==2.2.1
pyyaml==6.0.1
openpyxl==3.1.2
python-dotenv==1.0.1
pymongo[srv]==4.16.0
scikit-learn==1.6.1
